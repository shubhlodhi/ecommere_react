import { createContext } from "react";
// create context for passing the data to the other components



const contextbyme = createContext(
    {
        login:"dummy"
    }
)
export default contextbyme